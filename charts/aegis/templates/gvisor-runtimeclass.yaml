apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
scheduling:
  nodeSelector:
    sandbox-enabled: "true"
  tolerations:
  - key: "sandbox"
    operator: "Equal"
    value: "true"
    effect: "NoSchedule"
