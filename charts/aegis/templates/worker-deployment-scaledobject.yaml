apiVersion: apps/v1
kind: Deployment
metadata:
  name: aegis-worker
  namespace: aegis-workers
  labels:
    app: aegis-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aegis-worker
  template:
    metadata:
      labels:
        app: aegis-worker
    spec:
      serviceAccountName: aegis-worker-sa
      containers:
        - name: worker
          image: aegis/sandbox-worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: aegis-worker-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "2Gi"
          # Liveness/readiness probes help K8s recover unhealthy pods
          livenessProbe:
            httpGet:
              path: /health
              port: 6000
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 6000
            initialDelaySeconds: 10
            periodSeconds: 10

---
# KEDA ScaledObject - scales Deployment based on Redis list length (Celery queue)
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: aegis-worker-scaledobject
  namespace: aegis-workers
spec:
  scaleTargetRef:
    name: aegis-worker
  pollingInterval: 30  # seconds
  cooldownPeriod:  300
  minReplicaCount: 1
  maxReplicaCount: 20
  triggers:
    - type: redis
      metadata:
        address: "${REDIS_URL}"
        listName: "celery"
        listLength: "10"
        # If your celery queue uses a named list or different key, adjust listName
