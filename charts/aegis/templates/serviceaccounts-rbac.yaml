---
# -----------------------------------------------------------------------------
# Namespaces
# -----------------------------------------------------------------------------
apiVersion: v1
kind: Namespace
metadata:
  name: aegis-backend
---
apiVersion: v1
kind: Namespace
metadata:
  name: aegis-sandbox
  labels:
    pod-security.kubernetes.io/enforce: restricted

---
# -----------------------------------------------------------------------------
# Service Accounts
# -----------------------------------------------------------------------------
apiVersion: v1
kind: ServiceAccount
metadata:
  name: aegis-backend-sa
  namespace: aegis-backend
  annotations:
    description: "Used by the main FastAPI servers"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: aegis-worker-sa
  namespace: aegis-backend
  annotations:
    description: "Used by Celery workers to launch sandbox Jobs"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: aegis-sandbox-sa
  namespace: aegis-sandbox
  annotations:
    description: "Used by the ephemeral gVisor sandbox pods. Very restricted."

---
# -----------------------------------------------------------------------------
# RBAC: Allow Celery Workers to create/manage Jobs in aegis-sandbox ONLY
# -----------------------------------------------------------------------------
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: sandbox-manager-role
  namespace: aegis-sandbox
rules:
  - apiGroups: ["batch", "extensions"]
    resources: ["jobs"]
    verbs: ["create", "get", "list", "watch", "delete"]
  - apiGroups: [""]
    resources: ["pods", "pods/log"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: worker-sandbox-manager-binding
  namespace: aegis-sandbox
subjects:
  - kind: ServiceAccount
    name: aegis-worker-sa
    namespace: aegis-backend
  - kind: ServiceAccount
    name: aegis-backend-sa
    namespace: aegis-backend
roleRef:
  kind: Role
  name: sandbox-manager-role
  apiGroup: rbac.authorization.k8s.io

---
# -----------------------------------------------------------------------------
# RBAC: Restrict Sandbox Pods from accessing the Kubernetes API entirely
# -----------------------------------------------------------------------------
# By default, a SA can't do much, but we explicitly bind it to a role with no
# permissions just to be safe, and we rely on automountServiceAccountToken: false
# in the PodSpec.
