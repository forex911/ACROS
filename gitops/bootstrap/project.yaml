apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: aegis-project
  namespace: argocd
spec:
  description: "ArgoCD project for AegisAI applications"
  sourceRepos:
    - "REPLACE_WITH_REPO"
  destinations:
    - namespace: "*"
      server: "*"
  roles: []
