apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: sentinel-project
  namespace: argocd
spec:
  description: "ArgoCD project for SentinelAI applications"
  sourceRepos:
    - "REPLACE_WITH_REPO"
  destinations:
    - namespace: "*"
      server: "*"
  roles: []
