apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: acros-project
  namespace: argocd
spec:
  description: "ArgoCD project for ACROS AI applications"
  sourceRepos:
    - "REPLACE_WITH_REPO"
  destinations:
    - namespace: "*"
      server: "*"
  roles: []
