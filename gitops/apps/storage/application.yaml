apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aegis-storage-dev
  namespace: argocd
spec:
  project: aegis-project
  source:
    repoURL: "REPLACE_WITH_REPO"
    targetRevision: HEAD
    path: deployment/kubernetes/k8s/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: aegis-storage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
