apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: acros-workers-dev
  namespace: argocd
spec:
  project: acros-project
  source:
    repoURL: "REPLACE_WITH_REPO"
    targetRevision: HEAD
    path: deployment/kubernetes/k8s/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: acros-workers
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
